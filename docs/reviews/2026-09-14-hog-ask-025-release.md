---
title: "hog_ask 0.2.5 exact release verification"
date: 2026-09-14
status: locally_active_verified_publication_paused
---

# Exact 0.2.5 release candidate

The user requested publication and availability after accepting the native ordinary-question and approval flows. The remaining RPC/guidance work is now implemented and independently reviewed. This record is not publication or activation permission.

## Identity

- Package: `@heart-of-gold/toolkit@0.2.5`.
- Frozen source: `92bf1845540d3a462ceae59815d64ea1af35c9f9` (418 committed files).
- Artifact directory: `~/.local/lib/heart-of-gold/artifacts/0.2.5-92bf1845540d/`.
- Archive: `heart-of-gold-toolkit-0.2.5.tgz`, **551,957 bytes / 284 regular files**.
- SHA-256: `91ec56c4e43da61c43cc6a1eee8589c687b6ceff49da3b8e33e52c57c6a59c56`.
- SRI: `sha512-COCxtCZJlj5pNmkScMWGD8fbwoFF+JPRdiCdq/LPCxMgJHGx/HF89WkGeu2kzerKoHPYS8H4h0DZOnzf80BFtQ==`.
- Manifest SHA-256: `6887c2ed6b57837879b9806525d667efa385e41a05d351f10d943a81baeacf12`.

The archive was packed **once**, with scripts disabled after the explicit clean-export prepublish checks. Every archived path, byte and mode was verified against the committed export and expected npm manifest; no symlinks, path escapes, private `.pi/` state or untracked workstation drafts entered the package. Both development-only native/reference proof scripts are excluded and blocked by the safety checker. Dependencies, manifest resource lists and accepted native UI/core/schema/focus/presentation remain unchanged.

## Verification

| Check | Result |
| --- | --- |
| Red-first RPC tests against previous adapter | 17 pass / 9 fail |
| Focused native/core/RPC/package tests after correction | 75/75 |
| Clean-export safety, security and harness compatibility | PASS |
| Clean-export interaction / Pi / workstation / visualization | 37 / 167 / 148 / 2, all pass; no skips |
| Clean-export real Pi native PTY journeys | 40/40 |
| Extracted archive offline RPC / exact registration | 12/12 |
| Extracted archive real Pi native PTY journeys | 40/40 |

Pi/TUI **0.85.1**, Node **25.6.1**, existing local test dependencies and temporary offline profiles were used. No live model calls, dependency/runtime changes, global settings changes or forced reloads were needed. Archive proof fixtures live outside the candidate; only import/root paths and the explicit registration inspector were adapted, with the native assertions unchanged. Native-cell/PTY and static harness checks do not certify every physical keyboard, terminal, OS IME, RPC client or live-model workflow.

### Independent review

Existing Pi peer session `01a090e2-4ce8-77bc-aa39-85e0947ce1bf` returned **bounded static PASS, no actionable findings**, for frozen `b9cc226042b2da2d09a83aee5f093fdfa4d200fe..92bf1845540d3a462ceae59815d64ea1af35c9f9`. It inspected the RPC/metadata/packaging/test delta and relevant unchanged core/ownership boundaries. The parent independently checked the exact 11-file scope and reviewed blob identities.

Evidence covers decision-only direct submission, captured controller snapshots for custom→note, final cancellation, post-await abort checks, strict label-to-ID mapping, approval-only review with Send first, and explicit valid/invalid/whitespace feedback clearing. Substantive unit/RPC assertions cover counts, associations, validation, abort and ownership. Native bytes were independently compared to accepted `fc565ca7a034bf1947ab76347dccaf40c089ed38`.

The reviewer ran no tests, workers, native trials, web requests, settings/auth changes or edits. Its verdict does not certify archive execution or authorize publication. The previous native correction review and human acceptance remain separate evidence. The attempted background implementation helper failed audited runtime admission **before starting**; no alternate runtime was selected, and the parent implemented the change directly. The static review was a distinct task in an existing session.

### Retained failed checks and concurrent work

- Packaging preflight initially inferred root-bin executable modes incorrectly and failed before any artifact directory/archive was created. Inspection showed the existing npm archive mode behavior: `share-server/src/index.ts` is executable, while `src/index.ts` remains `0644`. The corrected preflight requires all 284 Git modes to equal the prior source and all npm modes to equal the previously verified archive. The actual archive independently matches these exact expectations; the initial failure is retained in the private receipt.
- A later unrelated-working-tree equality check failed because `workstation/README.md` changed during verification. Actor unidentified. No file was restored or overwritten. The other protected tracked hashes still matched at that observation, task-owned source files were unchanged, and the release remained the already frozen clean Git export. This does not claim the mutable workstation tree stayed frozen.

## Authorization and availability

At the candidate-proof checkpoint, nothing had been published or activated. Subsequently, **hog-025-publish-and-activate** returned **Approve this scope**, authorizing this exact archive's public npm publication with the latest tag and the separately named final source-only local activation. Noninteractive publication returned **EOTP**; a fresh registry read confirmed 0.2.5 absent. The normal npm browser approval page was opened for human authentication, without collecting an OTP or substituting/repacking bytes. The browser attempt subsequently timed out (npm exit `-15`); supervisor **50195**, runner **50197**, and npm **50198** were confirmed absent with ESRCH. Ondrej then paused publication and clarified that local use should not depend on it. No publisher or authentication retry remains running; a future publish needs a new user request and fresh collision/authentication checks. No successful npm publication is recorded. The rejected **0.2.4 archive remains stopped and untouched**.

### Local activation completed independently

The existing exact approval separately authorized local activation. npm publication was therefore removed as an unnecessary local-script prerequisite, without weakening source/archive checks or changing the artifact. The check-only preflight passed; the **284** verified committed files were copied to `~/.local/lib/heart-of-gold/releases/0.2.5-92bf1845540d` and sealed read-only. Only the existing `0.2.5-preview-fc565ca7a034` toolkit source in current global settings was replaced, using a same-directory atomic rename with compare-before-write checks. No stale settings backup was restored.

Fresh actual global-profile verification passed at **2026-09-14T21:15:43.490Z**: exact new launcher/skill paths, one `hog_ask`, unchanged workstation `/answer`, standard plan editor opened/cancelled, zero model calls, no extension/JSON/stderr errors, and owned verifier confirmed absent. The verifier left settings unchanged. A final audit at **21:16:41.436Z** reverified the complete sealed file set, both archives, all **four prior release trees**, and current settings differing only by the approved toolkit source. Theme and package/resource filters were preserved.

**Local 0.2.5 is available without npm publication.** New sessions use the final local release; existing sessions can use `/reload` when convenient. No reload was forced or attested for this conversation, and no additional acceptance ritual is required for the unchanged accepted native implementation.

[Machine-readable candidate checkpoint](2026-09-14-hog-ask-025-release-proof.json) preserves the pre-authorization state. Private `receipt.json` carries subsequent execution evidence. The older [native-source proof](2026-09-14-hog-ask-question-ux-proof.md) and [accepted preview activation](2026-09-14-hog-ask-025-preview-activation.md) are historical records, not identities or authorization for this new archive.
